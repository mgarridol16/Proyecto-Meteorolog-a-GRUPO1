{% extends "base.html.twig" %}
{% block content %}

<!-- Chart.js -->
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<section class="container my-5">
    <h1 class="text-center mb-5">🌡️ {{ titulo }}</h1>

    <!-- Última Lectura -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">📊 Última Lectura Registrada</h5>
                </div>
                <div class="card-body text-center">
                    {% if ultimaTemp %}
                        <h2 class="display-1 fw-bold text-primary">
                            {{ ultimaTemp.temperatura|number_format(1, ',', '.') }}°C
                        </h2>
                        <p class="text-muted fs-5 mb-4">
                            📅 {{ ultimaTemp.fechaSistema|date('d/m/Y H:i') }}
                        </p>
                        
                        <div class="row text-center">
                            <div class="col-md-3 mb-3">
                                <span class="badge bg-info fs-6 p-3">
                                    💧 Humedad: {{ ultimaTemp.humedad }}%
                                </span>
                            </div>
                            <div class="col-md-3 mb-3">
                                <span class="badge bg-warning text-dark fs-6 p-3">
                                    🌡️ Presión: {{ ultimaTemp.presion }} hPa
                                </span>
                            </div>
                            <div class="col-md-3 mb-3">
                                <span class="badge bg-success fs-6 p-3">
                                    💨 Viento: {{ ultimaTemp.viento }} km/h
                                </span>
                            </div>
                            <div class="col-md-3 mb-3">
                                <span class="badge bg-primary fs-6 p-3">
                                    🌧️ Lluvia: {{ ultimaTemp.lluvia ?? 'N/A' }} mm
                                </span>
                            </div>
                        </div>
                    {% else %}
                        <p class="text-muted fs-5">⚠️ No hay datos disponibles</p>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

    <!-- Gráfico -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">📈 Evolución de la Temperatura</h5>
                </div>
                <div class="card-body">
                    <canvas id="graficoTemperatura" height="100"></canvas>
                </div>
            </div>
        </div>
    </div>

    <!-- Info -->
    <div class="row">
        <div class="col-12">
            <div class="alert alert-info">
                <strong>ℹ️ Información:</strong> Los datos mostrados corresponden a las últimas mediciones de la estación meteorológica del IES Gregorio Prieto (últimos 30 días).
            </div>
        </div>
    </div>

</section>

<!-- Script Chart.js -->
<script>
    const datosTemperatura = {{ temperaturas|json_encode|raw }};
    
    if (datosTemperatura && datosTemperatura.length > 0) {
        const ctx = document.getElementById('graficoTemperatura').getContext('2d');
        
        new Chart(ctx, {
            type: 'line',
            data: {
                labels: datosTemperatura.map(d => d.fecha),
                datasets: [{
                    label: 'Temperatura (°C)',
                    data: datosTemperatura.map(d => parseFloat(d.temperatura)),
                    borderColor: '#95BF39',
                    backgroundColor: 'rgba(149, 191, 57, 0.2)',
                    borderWidth: 2,
                    tension: 0.4,
                    fill: true
                }]
            },
            options: {
                responsive: true,
                plugins: {
                    legend: {
                        display: true,
                        position: 'top'
                    }
                },
                scales: {
                    y: {
                        beginAtZero: false,
                        title: {
                            display: true,
                            text: 'Temperatura (°C)'
                        }
                    },
                    x: {
                        title: {
                            display: true,
                            text: 'Fecha'
                        },
                        ticks: {
                            maxRotation: 45,
                            minRotation: 45
                        }
                    }
                }
            }
        });
    } else {
        document.getElementById('graficoTemperatura').parentElement.innerHTML = 
            '<div class="alert alert-warning">⚠️ No hay datos disponibles</div>';
    }
</script>
{% endblock %}